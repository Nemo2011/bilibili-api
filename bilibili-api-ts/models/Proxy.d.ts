export declare class Proxy {
    hostname: string;
    port: string;
    username: any;
    password: any;
    constructor({ host, port, username, password }: {
        host: string;
        port: string;
        username?: string;
        password?: string;
    });
}
